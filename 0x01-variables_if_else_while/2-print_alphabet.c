#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 *
 * Discription: Get a random it with 5 
 * 
 * Retern: Always 0(Success)
*/

int main(void)
{
        char c = 'a';

	while (c <= 'z')
        {
		putchar(c);
		c++;
	}
	putchar('\n')
        return (0);
}
