#include "main.h"

/**
 * mul - a function that multiplies two integers
 *
 * @a: an int to ne multiplied and print
 * @b: an int to be multiplied and print
 *
 * Return: mul
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
